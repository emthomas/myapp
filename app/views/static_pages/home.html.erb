<% provide(:title, "Home") %>
<div class="center jumbotron">
  <h1>Welcome to Sam & Seb's Website</h1>

  <h2>
  </h2>
  <% if logged_in? %>
  <%= link_to "Wedding Info", wedding_path, class: "btn btn-lg btn-primary" %>
  <% end %>
  
	<%= image_tag(random_pic, :height => "auto", :width => "100%", alt: random_pic) %>
  
</div>
